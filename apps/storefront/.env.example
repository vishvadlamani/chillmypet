# Copy to .env for local development. Never commit the real file.

# --- Tenant ---
# Which store to serve when the Host header doesn't match a stores.domain row
# (localhost, preview URLs). Production resolves by Host and ignores this.
DEFAULT_STORE_ID=chillmypet

# --- Database ---
# Without these the app falls back to a local SQLite file (local.db), which is
# enough to run everything offline.
# Get both from: turso db show <name> / turso db tokens create <name>
TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=

# --- Meta Conversions API ---
# SECRET. Events Manager > Settings > Conversions API > Generate access token.
# A token authorises one dataset, so generate it from the dataset META_PIXEL_ID
# names; presented against any other, Meta refuses every event.
META_CAPI_ACCESS_TOKEN=

# Pixel ids are public and ship to the browser by design. Production keeps them
# as vars in wrangler.toml; set them here for local dev and the e2e suite.
META_PIXEL_ID=1363695699271757
# Further datasets that also receive the server-side copy, each with its own
# token paired by suffix. _3 through _9 work the same way.
META_PIXEL_ID_2=1341978141149107
META_CAPI_ACCESS_TOKEN_2=
# Browser events only, no CAPI. Comma-separated.
META_EXTRA_PIXEL_IDS=28272021345717397

# Optional. Routes events to Events Manager > Test Events instead of counting
# them as real conversions. Get the code from the Test Events tab. A code is
# issued by one dataset and means nothing to another, so each has its own.
META_CAPI_TEST_EVENT_CODE=
META_CAPI_TEST_EVENT_CODE_2=

# Optional overrides.
META_CAPI_API_VERSION=v25.0
# Point at a CAPI Gateway, a proxy, or a local mock. Defaults to graph.facebook.com.
META_CAPI_ENDPOINT=
# Sent as attribution_data.attribution_share when set.
META_ATTRIBUTION_SHARE=

# In production these are Worker secrets, not a file:
#   npx wrangler secret put TURSO_DATABASE_URL
#   npx wrangler secret put TURSO_AUTH_TOKEN
#   npx wrangler secret put META_CAPI_ACCESS_TOKEN
#   npx wrangler secret put META_CAPI_ACCESS_TOKEN_2
